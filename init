#!/bin/bash

source .env

mkdir -p $CHEMBIENCE_HOME && mkdir -p $APP_HOME

docker-compose -f ./context/base/docker-compose.build.yml run --rm base
docker-compose -f ./context/base/django/docker-compose.init.yml run --rm django


