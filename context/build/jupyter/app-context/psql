#!/bin/bash
set -e

source .env

if [ $# -eq 0 ]
  then
    docker-compose exec db psql -h postgres -U $CHEMBIENCE_DB_USER $CHEMBIENCE_DB_NAME
  else
    docker-compose exec db psql -h postgres -U $CHEMBIENCE_DB_USER $CHEMBIENCE_DB_NAME $1
fi

